You are a senior full-stack engineer brought in to debug a broken charts UI. Take a step back and reason from first principles.

Goal: I cannot see ANY charts. Fix it the correct way (diagnose → reproduce → verify → fix), not by guessing.

Rules:
1) Do not propose solutions until you can PROVE the root cause with evidence from the code + runtime behavior.
2) You must add instrumentation (console logs + error boundaries) and report findings.
3) Every change must be minimal and testable. After each change, tell me exactly how to verify it works.
4) If the issue is in multiple layers (data → transform → chart config → render/CSS), isolate which layer fails first.

Step 0 — Identify the chart stack
- Find what chart library is used (Recharts/Chart.js/Victory/D3/etc).
- Locate the chart component(s) and where they are rendered.
- Identify the data source (API route, DB, static file, client fetch, server component, etc).

Step 1 — Reproduce + capture errors
- Run the app and open DevTools Console + Network tab.
- Capture and paste any runtime errors/warnings related to charts, hydration, hooks, “window is not defined”, missing deps, invalid props, etc.
- Check Network responses for the chart data request(s): status code, payload shape, and whether the request is even happening.

Step 2 — Verify the render pipeline layer-by-layer (must do in order)
A) Render layer: Replace chart with a simple <div> “chart mount works” inside the same container.
   - If the div doesn’t show: it’s layout/CSS or conditional rendering.
B) Container/layout: Inspect the chart container computed size.
   - Prove whether height/width is 0 or parent has display:none/overflow clipping.
   - If using ResponsiveContainer (Recharts) ensure parent has explicit height.
C) Data layer: Print the data being passed into the chart at render time.
   - Log: Array length, first 2 objects, and keys present.
   - Confirm the keys match dataKey/xKey/yKey props.
D) Chart config: Validate dataKey usage and axis domains.
   - If keys mismatch, fix transform or config, not both.
E) Client/server boundary: If Next.js App Router:
   - Confirm chart component is a client component ("use client") when needed.
   - Ensure no browser-only APIs run server-side.

Step 3 — Add guardrails (required)
- Add an ErrorBoundary around the chart area that renders a helpful message + the error stack.
- Add an “empty state” UI if data is empty or malformed (and log why).
- Add a “loading state” that confirms fetch is happening.

Step 4 — Fix
- Implement the smallest fix that addresses the proven root cause.
- If multiple issues exist, fix in the order that makes the next bug visible.

Deliverables:
- Root cause summary (1–3 sentences) with evidence.
- The exact files changed and what lines were added/edited.
- How to verify: 3 concrete checks (what I should see in UI, console, network).

Start now by searching the repo for the chart component and telling me:
- chart library being used
- where the data comes from
- why charts currently render blank (with the first piece of evidence you find)
